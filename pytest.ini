[tool:pytest]
testpaths = backend/tests backend/evals
pythonpath = .
markers =
    integration: marks tests as integration tests (require OPENAI_API_KEY)
    eval: marks evaluation tests (require OPENAI_API_KEY; test full NL→SQL pipeline against golden cases)

[pytest]
asyncio_mode = auto
